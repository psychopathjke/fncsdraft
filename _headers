# Cloudflare Pages: как кэшировать то, что отдаётся с сайта.
#
# Скрипты запрашиваются с ?v=<хеш содержимого> (см. check-deploy-folder.js —
# он не даст хешу отстать), поэтому новый файл всегда приезжает по новому
# адресу, а старый можно держать вечно. Без этого Pages отдаёт всё с
# max-age=0, must-revalidate и БЕЗ ETag: приложение на полтора мегабайта
# качается заново при каждом заходе. На тонком канале это и выглядит как
# «сайт не грузится».
/app.js
  Cache-Control: public, max-age=31536000, immutable
/zone-sim.js
  Cache-Control: public, max-age=31536000, immutable
/zone-replay.js
  Cache-Control: public, max-age=31536000, immutable
/mp.js
  Cache-Control: public, max-age=31536000, immutable
# Воркер — наоборот, НИКОГДА не кэшируется. Браузер сверяет sw.js при каждом
# заходе, и если отдать его надолго, новая сборка не доедет до тех, у кого он
# уже стоит: старый воркер продолжит отдавать старые кэши. Это же — дорога
# назад, если воркер придётся снять (см. комментарий в самом sw.js).
/sw.js
  Cache-Control: no-cache, max-age=0, must-revalidate
# maps.js сюда НЕ входит: он подгружается как el.src='maps.js', без ?v —
# вечный кэш заморозил бы старую копию. Появится хеш — можно добавить.
# Шрифт лежит под своим именем и не меняется — см. комментарий в <head>.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable
# КАРТИНКИ. Observatory зоны, 5 сентября 2026: попаданий в кэш Cloudflare —
# 5,23%, остальные 94,77% идут на origin. Причина простая: всё, чего нет в
# этом файле, Pages отдаёт с max-age=0 — то есть каждый заход перепроверяет
# на сервере каждый портрет, герб и обложку по отдельности. Для того, у кого
# дорога до Cloudflare плохая, это сотни лишних кругов.
#
# Неделя, а не год: имя файла тут не меняется при замене содержимого (портрет
# игрока может быть переснят), и вечный кэш заморозил бы старую картинку у
# всех, кто её однажды видел. Хешей в этих именах нет — значит и immutable
# ставить нельзя.
/art/*
  Cache-Control: public, max-age=604800
/photos/*
  Cache-Control: public, max-age=604800
/logos/*
  Cache-Control: public, max-age=604800
/items/*
  Cache-Control: public, max-age=604800
/devices/*
  Cache-Control: public, max-age=604800
